spring:
  config:
    import: ""
  cloud:
    config:
      enabled: false
eureka:
  client:
    enabled: true
    register-with-eureka: false
    fetch-registry: false
    should-unregister-on-shutdown: false
